# Core
langgraph>=0.2.0
langchain-anthropic>=0.3.0
langchain-core>=0.3.0
anthropic>=0.40.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# API
fastapi>=0.110.0
uvicorn>=0.27.0

# Document output
python-docx>=1.1.0
# weasyprint>=62.0  # Optional — requires system libraries (cairo, pango). PDF falls back to DOCX if unavailable.

# Persistence (required when STORAGE_BACKEND=persistent; imported lazily)
redis>=5.0.0
psycopg[binary]>=3.1.0
psycopg-pool>=3.2.0
langgraph-checkpoint-postgres>=2.0.0
boto3>=1.34.0  # only needed when DOCUMENT_STORAGE=s3 (imported lazily)

# Utilities
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
